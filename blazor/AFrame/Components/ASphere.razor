<a-entity sq-collider geometry="primitive: sphere" material="color: @Color; opacity: @Opacity;" position="@P" width="@S.Width" height="@S.Height" />

@code {

	[Parameter]
	public string? Color { get; set; } = null;

	[Parameter]
	public double Opacity { get; set; } = 1.0;

	private Position P;
	private Size S;

	public ASphere() { P = new(); S = new(); }

	public ASphere(string? color = null, double opacity = 1.0,
		double posX = 0, double posY = 0, double posZ = 0,
		double width = 1, double height = 1)
	{
		Color = color;
		Opacity = opacity;
		P = new(posX,posY,posZ);
		S = new(width,height);
	}

}
